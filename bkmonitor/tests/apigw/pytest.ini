[pytest]
# 独立的 APIGW 测试配置，不依赖 Django
console_output_style = count
testpaths = tests
python_files = test_*.py
log_level = INFO
filterwarnings =
    ignore::DeprecationWarning
    ignore::ImportWarning
    ignore::ResourceWarning

# .env 文件已在 conftest.py 中通过 python-dotenv 加载
